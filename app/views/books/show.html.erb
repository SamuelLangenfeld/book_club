<p id="notice"><%= notice %></p>


<div class="col-md-4 col-md-offset-4 col-sm-4 col-sm-offset-4" >
  <div class="row">
  <div class="content">
    <div class="heading">
      <h1><%= @book.title %></h1>
      <h2>by <%= @book.author_first_name+" "+@book.author_last_name %></h2>
    </div>
  </div>
  </div>
  <div class="row">
    <div class="content">
      <div>
        <% if @book.meeting.nil? && user_signed_in? %>
          <%= link_to raw('<span class="glyphicon glyphicon-plus"></span>Add A Meeting For This Book'), new_meeting_path(:book_id=> @book.id)%>
        <% end %>
      </div>
      <div>
      <% if user_signed_in? %>
        <%= link_to 'Edit', edit_book_path, {:book_id=>@book.id} %> |
      <% end %>
      <%= link_to 'Back', books_path %>
      </div>
  </div>
</div>
